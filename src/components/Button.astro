---
const { text, isFilled, isTransparent } = Astro.props;
---

<button
  class:list={[
    'btn title-base',
    {
      btnFilled: isFilled,
      btnTransparent: isTransparent,
    },
  ]}>{text}</button
>

<style lang="scss">
  .btn {
    padding: 0.9375rem;
    border-radius: 1.75rem;
    color: #fff;
    font-family: 'Ubuntu', system-ui, -apple-system, BlinkMacSystemFont,
      'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans',
      'Helvetica Neue', sans-serif;
    font-weight: 500;
  }

  .btnFilled {
    color: #ff505c;
    background-color: #fff;
  }

  .btnTransparent {
    border: 1px solid #fff;
    background-color: transparent;
  }
</style>

